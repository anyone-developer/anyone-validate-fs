name: nightly-build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    name: run sample test locally
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - run: npm ci
      - name: Check xo formatter
        run: npm run xo
      - run: npm test